name: CI on Self-Hosted
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: runner1
    steps:
      - uses: actions/checkout@v4
      - name: Run a script
        run: echo "Running on my self-hosted runner!"





