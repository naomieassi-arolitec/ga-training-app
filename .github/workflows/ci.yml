name: "CI"

on:
  push:

jobs:
  quality:
    runs-on: 
      - self-hosted
      - label1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: "Setup Biome"
        uses: biomejs/setup-biome@v2
        with:
          version: "2.0.5"
      - name: "Run Biome"
        run: "biome ci ."
